server:
  port: ${PORT:8080}

spring:
  application:
    name: locadora-backend

  datasource:
    url: jdbc:postgresql://db.lsivnowjpaqwjlvzgcpl.supabase.co:5432/postgres
    username: postgres
    password: 090909MATHEUS@@@!
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.PostgreSQLDialect